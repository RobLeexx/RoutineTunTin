import { View } from 'react-native';

import { AppText } from '../atoms/AppText';

export function HomeHeader() {
  return (
    <View>
      <AppText variant="hero">Routine TunTin</AppText>
    </View>
  );
}
